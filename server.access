RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{DOCUMENT_ROOT}/repo/public/$0 -f
    RewriteRule ^.+\.(jpg|jpeg|gif|png|ico|css|js|swf|woff|woff2|ttf|eot)$ /repo/public/$0 [L]
RewriteRule .* repo/public/ [L]
